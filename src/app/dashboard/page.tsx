// /dashboard -- Staff dashboard home (protected)
// Server Component: verify auth session, fetch all items + latest borrows
// Renders: StatCards, OverdueAlert, ItemsTable
export default async function DashboardPage() {
  // TODO: createSupabaseServerClient() -> check session -> redirect to /login if none
  return <div>Dashboard -- TODO</div>
}