'use client'
// BorrowForm -- shown when item is AVAILABLE
// Props: itemId, itemName
// Fields: borrowerName, department (select), expectedReturn (date picker)
// On submit: POST /api/borrow
export {}